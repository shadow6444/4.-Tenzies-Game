This is a tenzies game for children. In this game player has to hold the same die and win the game with best score.

first install the vite packages by using
npm install vite --save-dev

and then run it using

npm run dev
